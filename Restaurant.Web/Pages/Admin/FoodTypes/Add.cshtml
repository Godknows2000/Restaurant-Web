@page
@model AddModel

<form method="post" class="ps-5">
        <h3 class="mt-5">Add new..</h3>
    <div class="card col-md-4 mt-1">
        <div class="card-header bg-secondary">

        </div>
        <div class="card-body">
            <div asp-validation-summary="All"></div>
            <div class="form-floating">
                <input asp-for="FoodType.Name" name="Name" class="form-control" type="text" />
                <label>Name</label>
                <span class="text-danger" asp-validation-for="FoodType.Name"></span>
            </div>
        </div>
        <div class="card-footer text-end">
            <a asp-page="Index" class="btn btn-secondary" style="width:150px">Back to list</a>
            <button type="submit" class="btn btn-success"><i class="fa-solid fa-floppy-disk"></i> Submit</button>
        </div>
    </div>
</form>
@section scripts{
    @{
        <partial name="_ValidationScriptsPartial"/>
    }
}